module.exports = Object.freeze({
  VALIDUPDATESUSER: [], // Array of fields that the user can modify on the User model
  VALIDUPDATESTASK: [], // Array of fields that the user can modify on the Task model
  BUCKET: "", // AWS S3 bucket name
  DEFAULT_AVATAR: "", // URL to the default user avatar (when user hasn't provide any)
});
